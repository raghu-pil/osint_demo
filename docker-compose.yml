services:
  authintify:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Persistent case storage — all investigations saved here
      - ./cases:/app/cases
    restart: unless-stopped
    environment:
      - PORT=8000
