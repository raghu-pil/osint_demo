# Core web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
python-multipart==0.0.9
aiofiles==23.2.1

# HTTP + parsing
requests>=2.31.0
urllib3>=2.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Chromium TLS fingerprint (Instagram share trace)
curl_cffi>=0.7.0

# YAML config
PyYAML>=6.0

# URL analysis
tldextract>=5.1.2

# Media download
yt-dlp>=2024.5.27

# Image metadata (EXIF)
Pillow>=10.0.0
piexif>=1.1.3

# Audio metadata
mutagen>=1.47.0

# Perceptual hashing for reverse image search
imagehash>=4.3.1

# SerpAPI — Google Lens + Yandex reverse image search, repost detection
google-search-results>=2.4.2

# Geolocation
geopy>=2.4.1

# PDF report generation
reportlab>=4.1.0

# Entity extraction (NER)
spacy>=3.7.0

# Username enumeration
sherlock-project>=0.14.3

# Phone number parsing (identity pivots)
phonenumbers>=8.13.0

# OCR (also needs: apt install tesseract-ocr)
pytesseract>=0.3.10

# Tor SOCKS proxy support
PySocks>=1.7.1

# LLM analysis
anthropic>=0.28.0

# Pretty output
rich>=13.0.0
